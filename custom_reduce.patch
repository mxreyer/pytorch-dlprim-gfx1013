diff --git a/src/kernels/reduce.h b/src/kernels/reduce.h
index 5b2ab19..0d4bce8 100644
--- a/src/kernels/reduce.h
+++ b/src/kernels/reduce.h
@@ -6,7 +6,13 @@
 ///
 ///////////////////////////////////////////////////////////////////////////////
 #ifndef CUSTOM_REDUCE
-#define CUSTOM_REDUCE 0
+// Forced on for Mesa rusticl (BC-250): the OpenCL 2.0 work_group_reduce_add/
+// max builtins below are a driver feature gap. rusticl does not advertise
+// __opencl_c_work_group_collective_functions and no libclc, upstream or
+// Mesa's patched fork, implements them, so the kernels fail to compile with
+// either. This falls back to the portable local-memory reduction, which needs
+// nothing beyond OpenCL 1.2. See README.md in mxreyer/pytorch-dlprim-gfx1013.
+#define CUSTOM_REDUCE 1
 #endif
 
 #define my_get_local_wg_id() ((get_local_id(2) * get_local_size(1) * get_local_size(0)) + (get_local_id(1) * get_local_size(0)) + get_local_id(0))
